/*project name-multiplication table upto given range of a given number*/
#include<stdio.h>
int main()
{
	int i,n,range;
	printf("Enter an integer: ");
	scanf("%d",&n);
	printf("Enter range: ");
	scanf("%d",&range);
	for(i=1; i<=range; i++)
	{
		printf("%d*%d=%d\n",n,i,n*i);
	}
	return 0;
}
